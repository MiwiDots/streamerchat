2026-05-31 22:20 - v0.3.28 (stable): 7TV per-user HTTP fallback + diag logs
  - 7TV cosmetics weren't showing on most usernames. The EventAPI
    WebSocket subscribe condition shape isn't well documented, so
    even when we connected the entitlement.* dispatches may have
    been ignored. Added a parallel /v3/users/twitch/<id> per-user
    fetch that fires the moment we see a Twitch chat message from a
    user we haven't queried yet. The HTTP endpoint's schema is
    stable so paint+badge data lands reliably. Backend dedupes per
    user-id so a chat burst doesn't fire many requests.
  - Verbose 7TV diagnostic logs: each dispatch is logged with type +
    first 400 chars of body so we can see exactly what 7TV pushes
    over the wire and harden the parser. Subscribe frames also log
    once per channel.

2026-05-31 22:00 - v0.3.27 (stable): Tab autocomplete for slash commands
  - Chat input in streamerchat-gui intercepts Tab when the line
    starts with "/" and there's no space yet. Pressing Tab cycles
    through every command whose name starts with the typed prefix:
    /v Tab → /vip → /unvip (Shift+Tab walks backwards). Any other
    keypress resets the cycle so the next /v Tab restarts from a
    fresh prefix.
  - Command list mirrors internal/twitch/slash.go so it stays in
    sync with what the backend actually accepts. Synonyms (/so for
    /shoutout, /subs for /subscribers) are included so they
    autocomplete too.

2026-05-31 21:40 - v0.3.26 (stable): proper app icons
  - New ChatHub icon: three overlapping chat bubbles in Twitch
    purple / YouTube red / Kick green with a central white hub dot,
    representing the multi-platform chat aggregator concept.
  - New StreamerChat icon: a single large purple chat bubble with
    three message lines and a red "live" recording dot in the
    upper-right corner.
  - Designed as SVG (sources in assets/icons/) and rendered through
    librsvg → 1024x1024 appicon.png, multi-resolution Windows .ico,
    and Mac .iconset → .icns for both apps. The .app bundles'
    Resources/iconfile.icns is updated too so Finder + the Dock pick
    up the new artwork on launch.

2026-05-31 21:15 - v0.3.25 (stable): slash commands in streamerchat-gui
  - New internal/twitch/slash.go: parses Twitch-style /commands and
    routes them through the matching Helix endpoint. Twitch
    deprecated raw IRC mod commands (PRIVMSG #ch :/ban foo) in early
    2023; all of these now go through Helix.
  - Supported: /mod /unmod /vip /unvip /ban /unban /timeout
    /untimeout /clear /slow /slowoff /followers /followersoff
    /subscribers /subscribersoff /emoteonly /emoteonlyoff /announce
    /announceblue /announcegreen /announceorange /announcepurple
    /raid /unraid /shoutout (/so), plus /help to list them.
  - Username arguments are resolved to numeric Twitch user ids via
    Helix /users on the fly. Duration arguments accept plain seconds
    ("60"), "5m", "1h", "1d".
  - Status (success or error string) is rendered as a system message
    in the chat view so the user sees what happened without leaving
    the chat input.

2026-05-31 20:30 - v0.3.24 (stable): 7TV paints + badges
  - New internal/sevenTV package: single WebSocket connection to
    wss://events.7tv.io/v3, subscribes to entitlement.* per Twitch
    broadcaster id, parses paint/badge entitlements off the wire and
    pushes them down to the frontend as Wails "sevenTVCosmetic"
    events. Per-channel HostID subscription happens lazily in
    resolveChannelID once we know a channel's Twitch user id; the
    streamer-gui app subscribes the broadcaster's own channel once
    auth validates.
  - Paint definitions (gradients, solid colors, drop shadows) are
    fetched in bulk from https://7tv.io/v3/cosmetics every 30 minutes
    and converted to CSS rules — linear/radial gradients become
    background + -webkit-background-clip:text, solids stay as
    plain color, paint shadows turn into text-shadow.
  - Both apps' chat-message render path now looks up the sender's
    Twitch user_id in the cosmetic map: if a paint is present, the
    username span gets the gradient/animated paint instead of the
    flat Twitch color; if a badge is present, it renders as a small
    image right before the username with the badge's tooltip.
  - Falls back gracefully — users without a 7TV subscription render
    exactly as before, no extra network hits per message.

2026-05-31 19:55 - v0.3.23 (stable): scroll-follow sticky + font-size + gui i18n
  - Scroll-follow no longer measures geometry on every append (which
    broke under ctrl+wheel zoom and tall multi-line messages — the
    classic "chat freezes until I scroll to the bottom manually"
    bug). Replaced with a sticky boolean per chat view that flips
    only from the user's own scroll input, plus a 40px slack window
    so rounding/image-reflow can't kick us out of follow mode.
  - Chat font size is now a real setting (Settings → General →
    Chat font size, range 10–28 px, default 14). Backend persists
    it; CSS var --chat-font-size drives every .msg so the whole
    chat rescales atomically. Ctrl + / Ctrl - / Ctrl 0 shortcuts
    work as expected, and Ctrl+mousewheel maps onto our slider
    instead of WebView2's native zoom — which was the original
    cause of the scroll-freeze regression.
  - streamerchat-gui got i18n: English + German bundles, Language
    dropdown in Settings → General, hydrated from the new
    cfg.UI.Locale field. The settings modal is fully translated;
    chat content + mod actions stay english for now.
  - Both apps share the new font-size + sticky-scroll behaviour.

2026-05-29 02:45 - v0.3.22 (stable): rollup of the v0.3.x alpha line
  Headline changes since v0.2.27 (last stable):
  - Twitch send via Helix POST /chat/messages instead of raw-IRC
    PRIVMSG (Twitch's own docs call IRC "historically supported" and
    Chatterino moved off it for the same reason — silent drops after
    idle, no NOTICE feedback). Needs a one-time re-login on Twitch
    to grant the new user:write:chat scope. IRC stays for read.
  - YouTube live chat: read + send via InnerTube cookie auth, with
    a LaunchLoginFlow that spawns an isolated Edge/Chrome window and
    pulls the session cookies out of the browser's SQLite store
    (no chromedp / no CDP automation Google would block). Auto-
    refresh every 24h keeps the session warm. Send is gated by the
    sendLiveChatMessageEndpoint extracted from the authenticated
    initial-data render.
  - Token storage moved out of plaintext config.json into the OS
    keyring (Windows DPAPI + per-user file, macOS Keychain, Linux
    libsecret), with one-shot migration for existing installs.
  - Self-updater channels: stable / beta / alpha, picked per-user
    from Settings → Updates. Alpha testers now get -alpha builds
    via the in-app updater instead of manual downloads.
  - Settings UI is tabbed in both apps (General / Sound / Twitch /
    YouTube / Updates / About in streamerchat-gui; chathub already
    had its tabbed layout).
  - Chat history persistence: per-channel .jsonl restored from disk
    on startup; filenames sanitized so YouTube tab keys (containing
    colons) actually persist on Windows.
  - Lots of smaller fixes shipped during the alpha:
      auto-scroll for tall multi-line messages, YT poll loop that
      refreshes a stale continuation, liveStatusLoop no longer wipes
      YT live-dots, IRC send no longer races its JOINs, chat history
      load no longer races init, no more "Joined #" / "Connected
      to" spam on startup, tab-list no longer flashes all tabs
      visible before hiding offline ones, scrollbar polish on the
      settings modal, join/part toggle (default ON) in the gui.
  See the v0.3.0-alpha through v0.3.21-alpha entries below for the
  per-release detail.

2026-05-29 02:30 - v0.3.21-alpha: hide stray vertical scroll arrows on settings tab bar
  - Settings tab bar in both apps showed Windows WebView2's vertical
    scroll arrows at the right edge even when only horizontal scroll
    was meaningful. Added scrollbar-width:none + a webkit override
    so the tab row scrolls horizontally when too many tabs to fit
    but nothing else paints scrollbar UI.

2026-05-29 02:15 - v0.3.20-alpha: gui settings scrollbar polish
  - Settings modal in streamerchat-gui was showing an empty outer
    scrollbar on Windows WebView2 even when every pane fit, because
    .modal had overflow:auto and its layout flexed past the height
    briefly while the tab-bar+pane+button-row stacked. Made the
    settings modal a proper flex column with overflow:hidden, and
    moved padding onto the header / pane / button-row so the active
    pane is the only scrollable region.

2026-05-29 02:00 - v0.3.19-alpha: streamerchat-gui tabbed settings + join/part toggle
  - streamerchat-gui's Settings modal was one long scroll. Now mirrors
    chathub's tabbed layout: General / Sound / Twitch / YouTube /
    Updates / About panes with a top tab bar.
  - General → Show join/part messages toggle (default ON, matches
    DefaultConfig). The joinPart event handler now skips the
    chat-line render when the toggle is off; the right-hand user list
    still updates because that's where you actually want to see
    who's around. New backend hooks: GetShowJoinPart, SetShowJoinPart.

2026-05-29 01:45 - v0.3.18-alpha: kill startup spam + fix tab-list flicker
  - At boot the IRC layer emitted one "Joined #channel" + one
    "Connected to #channel" system message per watched channel,
    drowning any historical chat the user just got back. Both
    emissions are gone — the channel tab + live indicator already
    say "we're watching this" and the IRC connect lifecycle now only
    logs to chathub.log.
  - Tab list flicker: applyState in the frontend was calling
    addChannelTab BEFORE assigning onlyShowLive, so the initial
    visibility pass saw onlyShowLive=false and rendered all 22 tabs.
    Half a second later the live poll flipped it to true and tabs
    started disappearing one by one. Now onlyShowLive (and the
    cached liveStatus map) get seeded BEFORE the channel-tab loop
    so offline channels are hidden on first paint.

2026-05-29 01:30 - v0.3.17-alpha: gui update-channel dropdown
  - streamerchat-gui got the backend hooks for update channels in
    v0.3.14 but no UI to flip them — only chathub had the dropdown.
    Now Settings → Updates in the gui app also has the channel
    selector with the same hydrate-on-open / persist-on-change wiring.

2026-05-29 01:20 - v0.3.16-alpha: fix history-nil race
  - The v0.3.15 diagnostics caught the actual bug: the frontend's
    setupEvents() pulls GetInitialState the moment the Wails runtime
    is up, which fans out into 22× GetHistory calls — all within the
    same millisecond startup() began. a.history was assigned LATER in
    startup(), so every one of those calls hit "a.history nil" and
    returned no messages. The chat view stayed empty even though
    miwitv.jsonl etc. on disk had thousands of lines.
  - Moved history := NewHistoryWriter() into the NewApp() constructor.
    Now the writer is set the moment the App struct exists, which is
    before Wails wires up any binding, so the race window is gone.

2026-05-29 01:15 - v0.3.15-alpha: extra history diagnostics
  - Added [HISTORY] GetHistory called log + console.log in
    loadHistoryForChannel so we could prove whether the frontend was
    even firing the call. Confirmed it was — racing with init.

2026-05-29 01:00 - v0.3.14-alpha: selfupdate channels (stable/beta/alpha)
  - selfupdate.LatestForChannel picks the newest release matching the
    chosen stream:
      stable → /releases/latest (prereleases excluded by definition)
      beta   → newest release whose tag doesn't contain "-alpha"
      alpha  → newest release at all, including prereleases
    A beta-channel user with no betas published yet falls back to the
    latest stable so the channel can't accidentally pin them on an
    older version.
  - HubConfig (chathub) and UIConfig (streamerchat-gui) gained an
    UpdateChannel field; GetUpdateChannel + SetUpdateChannel are
    exposed to the frontend.
  - chathub settings → Updates section now has a "Update channel"
    dropdown so the user picks once and "Check for updates" follows
    that stream. Future alpha releases get pulled by the existing
    Apply path — no more manual download dance.

2026-05-29 00:30 - v0.3.13-alpha: chat history filename sanitization
  - YouTube tab keys (e.g. "yt:@DEmiwitv") were passed through to
    history.Append/Load as-is. On Windows the colon is reserved (NTFS
    stream separator) so os.OpenFile failed silently — meaning YT chat
    was never persisted, and on restart Load returned nil with no
    visible error. Now every channel key passes through safeFilename
    which maps the reserved set (&lt;&gt;:"/\\|?*) to "_". Twitch channels
    like "miwitv" pass through unchanged so existing miwitv.jsonl
    files keep loading.
  - [HISTORY] load now logs the path and line count, so if Twitch
    history is still missing we can see whether the file was opened
    and how many lines came back.

2026-05-28 22:30 - v0.3.12-alpha: migrate Twitch send to Helix
  - Twitch officially flags IRC as "historically" supported in their
    docs and the preferred path is now "EventSub + Twitch API". The
    silent post-idle drops we've been chasing match Twitch's
    documented behaviour of throttling raw-IRC PRIVMSG for unverified
    clients while returning no NOTICE at all. Chatterino moved off
    IRC-send to the Helix endpoint for the same reason.
  - SendMessage now calls POST https://api.twitch.tv/helix/chat/messages
    with broadcaster_id + sender_id + message. The endpoint returns:
      * 200 with is_sent=true → message went out
      * 200 with is_sent=false + drop_reason → surfaced as an error so
        the UI shows "Twitch: This room is in follower-only mode" etc.
      * 4xx → HTTP body propagated as the error string
  - Required scope user:write:chat added to requiredScopes; users will
    be re-prompted on next Twitch login to grant it.
  - IRC connection stays alive — it's still our reader. Only the send
    path migrated. connectSendClient / NewIRCClient send-path code is
    still wired up so the read client and join/part handlers don't
    move.

2026-05-28 22:00 - v0.3.11-alpha: idle send dies — instrument + clear zombie
  - User reports Twitch send works at startup but silently stops after
    idle. gempir's Connect() blocks and is supposed to reconnect
    internally, but if it ever returns (terminal auth failure, ctx
    cancel, …) we kept a.send pointing at the dead client and every
    subsequent SayTo() vanished. Now: when the Connect goroutine
    returns we null out a.send so the next SendMessage falls into the
    lazy-connect branch and spins up a fresh IRCClient with the
    current token.
  - Added [SEND] OnConnect (logged the moment Twitch confirms auth +
    JOIN) and [SEND] OnReconnectMessage (logged when Twitch tells us
    to reconnect). Next idle-then-send test will show either:
      * OnConnect fires once, no later reconnects → Twitch is silently
        rejecting PRIVMSGs and the NOTICE handler (v0.3.10) should
        surface why.
      * OnReconnect or "connect returned" appears → gempir is cycling
        and the new code re-creates the sender.

2026-05-28 21:45 - v0.3.10-alpha: surface Twitch NOTICE rejects
  - PRIVMSGs were reaching the IRC layer (confirmed via the v0.3.9
    [SEND] logs) but disappearing without a trace. Twitch sends a
    NOTICE back with a msg-id tag (msg_followersonly, msg_subsonly,
    msg_banned, msg_duplicate, msg_ratelimit, etc.) explaining why,
    and we weren't listening for it. The send client now does:
    OnNoticeMessage logs the msg-id + reason and pushes the human-
    readable reason into the chat view as a system message so the
    user sees "Twitch: This room is in follower-only mode" instead of
    just nothing happening.

2026-05-28 20:55 - v0.3.9-alpha: diagnose Twitch send + join race
  - connectSendClient used to fire one goroutine for Connect() and a
    second one that slept 2s before issuing Join() for every secondary
    channel. If the IRC server hadn't authed us within 2s, those JOINs
    landed on an unauthenticated connection and were silently dropped —
    looked like "Twitch chat works for a while, then sending stops".
    NewIRCClient now takes the extra channels up front and the JOIN
    list is issued inline before client.Connect(), which the gempir lib
    delivers as soon as the auth handshake finishes (and re-delivers on
    every auto-reconnect, so we stay joined after network blips).
  - Added [SEND] logs on connect, on lazy-reconnect, and on every
    outgoing PRIVMSG so the next round of testing surfaces what
    actually reaches the IRC layer.

2026-05-28 20:30 - v0.3.8-alpha: auto-scroll + YT poll recovery
  - appendAndScroll measured "is the user at the bottom?" AFTER appending
    the new message, so a tall multi-line line bumped scrollHeight past
    the 50px tolerance and the auto-stick-to-bottom check dropped the
    user out of follow-mode. Snapshot the position BEFORE appending, and
    re-stick on the next animation frame so late-loading badges/emotes
    don't leave the view one image short.
  - InnerTube poll loop: when fetchMessages returns "no
    liveChatContinuation" three times in a row the saved token has gone
    stale and every subsequent poll fails silently — that's the
    "Chat hört nach einer Weile auf" symptom. Now refetches initial data
    in-place (silent mode, no extra system message) to grab a fresh
    continuation, then resumes polling.

2026-05-28 19:00 - v0.3.7-alpha: stop liveStatusLoop from clobbering YT tabs
  - The Twitch live poller iterated cfg.Channels without filtering, so
    every 60s it called Helix /streams with a "yt:@..." login, got a
    400 back, treated the YT channel as offline, and emitted
    liveStatus(false) — which wiped the live-dot the per-channel
    LiveDetector had set seconds earlier. Net effect: YT channels were
    permanently dark even when the stream was live.
  - liveStatusLoop now skips non-twitch entries. YT live state is owned
    by youtube.LiveDetector + the channel watcher, which is correct.
  - InnerTube connect errors are now logged in the retry loop so the
    "chat area empty even though stream is live" case has visible
    diagnostics next time.
  - cookieHeader() drops bytes that fall outside the RFC6265
    cookie-octet set. Without this, a stray high-bit byte in
    VISITOR_INFO1_LIVE (the Windows SQLite reader occasionally returns
    one) triggered "net/http: invalid byte 'ï'" and the whole Cookie
    header got rejected, which would have broken the new authenticated
    fetchInitialData path silently.

2026-05-28 18:30 - v0.3.6-alpha: fix send regressions on Twitch and YouTube
  - Twitch: connectSendClient was passing the first entry of
    cfg.Channels as the IRC primary, including YouTube ("yt:@handle")
    entries. The IRC client then tried to JOIN a YouTube tab key on
    Twitch chat, the connect attempt poisoned the send pipe, and every
    SayTo() silently dropped. Now filters to Twitch-only channels (via
    parseChannelRef) before connecting.
  - YouTube: InnerTubeClient.fetchInitialData was fetching the chat
    page anonymously, so YouTube rendered the read-only view that has
    no sendLiveChatMessageEndpoint. SendParams() came back empty and
    the frontend reported a misleading "sub-only / follower-only"
    error even for the channel's own owner. New SetCookies() attaches
    the saved session to the page fetch so the authenticated render
    (with the send endpoint) lands. app.go wires LoadLoginCookies into
    the per-channel watcher when spinning up the InnerTube client.

2026-05-28 18:00 - v0.3.5-alpha: fix "data passed to Set was too big" on Windows
  - Windows Credential Manager caps each entry's CredentialBlob at
    2560 bytes. A real YouTube session blob (with Secure-3PSID +
    Secure-1PSID + the rest) easily clears 4KB — keyring.Set returned
    "data passed to Set was too big" and the YT login flow failed at
    the very last step.
  - credstore now has a platform-specific backend:
      windows: DPAPI (CryptProtectData, current-user scope) +
               %APPDATA%/chathub/creds/<service>.bin
      darwin/linux: OS keyring (Keychain / Secret Service, no size limit)
  - Same crypto guarantee on Windows as Credential Manager (which uses
    DPAPI internally), no 2560-byte ceiling.

2026-05-28 17:30 - v0.3.4-alpha: YT auto-refresh + Twitch tokens to OS keyring
  - new internal/credstore: shared keyring wrapper. Service constants
    (chathub-twitch, chathub-youtube, chathub-kick) so the call sites
    use named enums instead of inline strings. Get / Set / Clear.
  - YouTube cookie auto-refresh: StartAutoRefresh runs from boot, fires
    once after 30s (validate + fold in fresh Set-Cookie values) and then
    every 24h. If YouTube redirects to ServiceLogin the keyring entry
    is cleared and authExpired surfaces. Reuses cookiejar so Google's
    rotating cookies actually get persisted instead of dropped.
  - LoginCookies struct gained SavedAt / RefreshedAt timestamps.
  - Twitch access + refresh tokens are migrated out of config.json into
    the OS keyring on first boot. Existing users keep their session —
    migrate-then-wipe leaves the in-memory tokens intact, the config
    file no longer carries the plaintext. New logins / refreshes write
    only to the keyring.
  - cmd/chathub/twitch_creds.go: small migrate / save / clear helpers
    wired into validateAndConnect's refresh path, the device-code
    login success path, and Logout.

2026-05-28 17:00 - v0.3.3-alpha: kill chromedp, read cookies directly from disk
  - Google blocks automation-controlled browsers ("Couldn't sign you in /
    This browser or app may not be secure") regardless of which flags we
    set. chromedp was always going to be a dead end for this flow.
  - Architecture change: spawn the browser via plain os/exec (no CDP, no
    automation flags, looks exactly like a user double-clicking the
    shortcut). User logs in normally. We poll the browser's own SQLite
    cookie database under the temp profile every 3s and decrypt values
    via Windows DPAPI + AES-256-GCM with the master key from Local State.
  - New internal/youtube/cookie_read_windows.go: SQLite read +
    CryptUnprotectData + AES-GCM decrypt. Pure Go (modernc.org/sqlite +
    golang.org/x/sys/windows). Stub returns clear error on non-Windows.
  - Cookie polling waits 4s after spawn so the browser has time to
    create the empty SQLite + write its first session row.
  - Preferred browser order on Windows is now Chrome → Edge → Brave
    (user pushed back: he uses Chrome). Falls back through the list if
    the first install isn't there.

2026-05-28 16:30 - v0.3.2-alpha: tabbed settings + browser fallback for YT login
  - Settings modal was scrolling forever once Twitch + YouTube + Updates +
    Debug sections piled up. Refactored into 6 tabs (General /
    Notifications / Twitch / YouTube / Updates / Debug) with a Chrome-like
    tab strip on top; only one pane is rendered at a time.
  - YouTube login retry: previously findChromiumBinary returned the FIRST
    install we saw and gave up if that one failed to spawn (user reported
    "chrome failed to start" even with Chrome actually running). Now we
    enumerate every candidate (Edge, Chrome, Brave, …) and try each one
    in turn until one launches, logging each attempt.

2026-05-28 16:00 - v0.3.1-alpha: ChatHub — YouTube SEND via InnerTube cookie auth
  - User pushed back on "why YouTube Data API at all" — research confirmed
    InnerTube /live_chat/send_message accepts the same cookie-based auth
    YouTube's own web client uses, no quota, no Google Cloud project.
    YouTube.js / kusaanko's lib use the same path in production.
  - New internal/youtube/login.go: cookie set struct + OS-keyring persistence
    (Windows Credential Manager / macOS Keychain / Linux Secret Service via
    zalando/go-keyring). Cookies never touch config.json.
  - New internal/youtube/send.go: computes the SAPISIDHASH Authorization
    header (timestamp + sha1) and POSTs to InnerTube live_chat/send_message
    with the right context payload + UA + Origin.
  - New internal/youtube/login_flow.go: spawns an isolated Edge / Chrome
    window (tmp --user-data-dir, headed) at the Google login URL via
    chromedp's CDP allocator. Polls Network.GetCookies every 2s until
    SAPISID lands, then snapshots SID/HSID/SSID/APISID/__Secure-3PSID/
    __Secure-3PAPISID/LOGIN_INFO/VISITOR_INFO1_LIVE and tears down the
    browser. tmp profile is wiped on exit so we don't leave login state.
  - innertube reader now extracts sendLiveChatMessageEndpoint.params on
    initial chat page render so SendLiveChatMessage knows which chat to
    post into. Per-tab InnerTubeClient kept in App.ytClients so send
    routing knows the current live stream.
  - App.go: new StartYouTubeLogin / LogoutYouTube / HasYouTubeLogin /
    SendYouTubeMessage bound methods. Frontend send-handler routes to
    SendYouTubeMessage when the active tab is a YT tab.
  - Settings modal: new "YouTube Login" section with Connect / Disconnect
    buttons and a status pill that lives in the same place as the Twitch
    pill. Updates immediately via the new youtubeLoginChanged event.

  Pre-release. Requires Edge or Chrome on the user's machine for the
  login window. Cookies live in the OS keyring under "chathub-youtube".

2026-05-28 11:00 - v0.3.0-alpha: ChatHub — experimental YouTube read support
  - Channels can now be Twitch OR YouTube. Storage stays []string but
    YouTube entries get a "yt:" prefix (e.g. "yt:@DEmiwitv"); legacy
    plain-string entries are still treated as Twitch so config files
    survive an upgrade.
  - new ChannelRef type, parseChannelRef helper, AddChannelOn(platform,
    name) bound method. AddChannel still works as the legacy entry.
  - Add-channel modal: radio buttons to pick Twitch / YouTube before
    typing the name / handle.
  - Boot iterates all stored refs and dispatches by platform: Twitch
    JoinChannel as before, YouTube spins up a LiveDetector + InnerTube
    chat poller per channel. Multiple YT channels run in parallel
    (own context.CancelFunc each).
  - Tabs get a TW/YT pill prefix; YT pill is red, TW is brand purple.
  - Stream meta banner is Helix-driven so it stays hidden on YT tabs.
  - Send: Twitch still works. YouTube send shows a "coming in next
    release" status — needs Google OAuth + YouTube Data API
    (LiveChatMessages.insert), tracked as v0.3.1.
  - Shipped as a PRE-RELEASE so the auto-updater does NOT pull it
    automatically. Grab the .exe manually from the release page to
    try.

2026-05-27 13:25 - v0.2.27: ChatHub — i18n for chatter-list strings + stream meta
  - Recent additions (chatter list modal, section titles, restriction note,
    search placeholder, offline label, button hover title) all shipped with
    hardcoded English. Moved every string into i18n.js with DE + EN
    counterparts and replaced the inline literals with i18n.t() calls.
  - No behavior change; just a cleanup so the German locale doesn't drop
    back to English in the new UI surfaces.

2026-05-27 13:15 - v0.2.26: chatter list — explain why lurkers may be missing
  - User noticed lurkers still don't appear. Cause is a Twitch policy
    limit, not a bug: /chat/chatters only returns the full list to the
    broadcaster or a moderator of the channel. Looking at someone else's
    channel returns 401/403 and we fall back to the locally-seen list.
  - Modal now shows an inline yellow note when we couldn't get the Helix
    list, so the missing lurkers don't look like a bug.

2026-05-27 13:05 - v0.2.25: ChatHub — separate Bots category, brighter icon
  - GetChannelChatters now also returns an isBot flag per user, sourced
    from the existing twitch.BotDetector (TwitchInsights community list
    loaded once at boot). Twitch's own API has no "bot" concept, so this
    is a curated external list rather than a heuristic; empty list on
    fetch failure means no false positives.
  - Frontend chatter list has a new "Bots" bucket. A bot that's also a
    mod (channels often promote their own bots) goes into the Bots bucket
    rather than Moderators — more useful for at-a-glance scanning.
  - Stream-meta 👥 button restyled with the brand purple background +
    white text so it pops instead of blending into the bar.

2026-05-27 12:55 - v0.2.24: ChatHub chatter list — Helix lurkers + always-visible icon
  - User feedback: previous version only showed people who had chatted in
    this session (lurkers missing), and the button was hidden when the
    stream was offline because the whole meta bar was hidden.
  - app.go: new GetChannelChatters bound method, hits Helix /chat/chatters
    (moderator:read:chatters scope, already in our token). Returns the
    full live viewer list when we're broadcaster / mod of the channel,
    or an empty list otherwise (silent 401/403 — falls back to local).
  - frontend: chatter list now merges Helix viewers (no roles) with our
    locally-tracked role data. Lurkers appear under "Chatters" without
    a role badge; chatters we've seen keep their broadcaster/mod/VIP/sub
    bucket. Helix call is cached per channel until next open.
  - Banner is no longer hidden when the channel is offline. It stays
    visible with greyed-out fields so the 👥 button is always reachable.
    Brightened the icon (purple-tinted background + border) so it pops.

2026-05-27 12:35 - v0.2.23: ChatHub — chatter list popup from the stream meta bar
  - 👥 button on the right side of the .stream-meta row opens a Chatterino-
    style chatter-list modal.
  - Locally-tracked chatters (already collected for @-autocomplete) are
    now also tagged with their IRC-derived role flags (isMod / isVIP /
    isBroadcaster / isSub) — sticky so a missing badge on a later message
    doesn't undo the role.
  - Categories: Broadcaster, Moderators, VIPs, Subscribers, Chatters.
    Click any name to open the user card. Search box on top filters live.
  - No Helix call — the official /chat/chatters endpoint needs a mod scope
    on the channel; using the local data works everywhere and is instant.

2026-05-27 00:25 - v0.2.22: ChatHub — Chatterino-style stream meta banner
  - Inspired by Chatterino's "Channel-Title (live) - <uptime> - <viewers> -
    <title>" header. We now show the same row under the tab bar in
    ChatHub for whichever channel is currently active, but only when it's
    actually live on Twitch.
  - internal/twitch: not touched — fetchStreamInfo is in chathub's app.go
    because that's where the existing checkLive lived. Now returns the
    full Helix /streams payload (title, game_name, viewer_count,
    started_at, language, thumbnail) instead of just a bool. checkLive
    became a thin wrapper that asks the same call for just the live bit.
  - new bound method GetStreamInfo(channel) for the frontend.
  - frontend/dist: new .stream-meta row with channel + game + uptime
    (computed client-side from started_at, ticks live), viewer count,
    title. Refreshes on tab-switch and every 60s.

2026-05-26 16:25 - v0.2.21: StreamerChat — per-message chat sound (WAV support)
  - internal/config: new UIConfig fields ChatSoundEnabled / ChatSoundFile /
    ChatSoundVolume (0-100).
  - cmd/wails/app.go: GetChatSoundConfig / SetChatSoundEnabled /
    SetChatSoundVolume / PickChatSoundFile / ClearChatSoundFile /
    LoadChatSoundData (returns the .wav as a data: URL so WebView2 can play
    it from the asset origin — file:// is blocked).
  - Settings modal: new "Chat sound" section with enable toggle, .wav file
    picker (native OpenFileDialog limited to *.wav), Clear button to fall
    back to a built-in oscillator beep, volume slider, and a Test button.
  - Per-message playback is throttled to 1 sound per 150 ms so a burst of
    messages doesn't melt the user's ears. Falls back to the built-in beep
    if no file is configured.
  - AudioContext is resumed on first user click/keydown (browser policy).

2026-05-26 16:10 - v0.2.20: cursor-bump on bubbling events (streamerchat fix)
  - v0.2.19 fixed chathub but not streamerchat. Reason: mouseenter and
    pointerenter don't bubble. streamerchat's layout has multiple nested
    containers that absorb those events before they reach the document
    listener.
  - Switched to bubbling counterparts (mouseover / pointerover / mousemove)
    plus focus / blur / keydown so the cursor reset fires on basically
    any interaction. requestAnimationFrame guard prevents thrashing —
    only one bump per frame.

2026-05-26 16:00 - v0.2.19: revert cursor-chasing changes, keep only the real fix
  - Once the real cause was identified (mouse-sharing software synthesizing
    incomplete mouse events), all the speculative changes can come out:
  - REVERTED: WebviewDisableRendererCodeIntegrity flag (was a security
    tradeoff for a theory that turned out wrong).
  - REVERTED: chathub liveStatusLoop parallelization + 60s->120s cadence
    (back to sequential + 60s — the original behavior was fine).
  - REVERTED: parallel emote lookups via Promise.all in renderText (back
    to sequential await — sequential is actually friendlier on Wails IPC
    since each round-trip yields the event loop).
  - REVERTED: loading="lazy" / decoding="async" on badge & emote images
    (no measurable impact, just code noise).
  - KEPT: the bumpCursorState handler on focus / mouseenter / pointerenter
    that's the actual fix.

2026-05-26 15:50 - v0.2.18: actual busy-cursor cause — mouse-sharing software
  - User figured out: with a normal mouse on the host PC, no spinner.
    With Synergy / Barrier / Mouse Without Borders / Logitech Flow
    (sharing the mouse across two PCs), every time the cursor crosses
    back into the app window the loading-spinner gets stuck.
  - Synthetic mouse events from those tools don't carry a clean
    mouseleave; WebView2 stays in a stale "navigating" cursor state
    until it gets a proper enter event. We now force a tiny cursor
    bump on window focus + document mouseenter + pointerenter to kick
    the renderer out of that state.
  - The v0.2.17 RendererCodeIntegrity flag is kept (cheap, no downside).

2026-05-26 15:40 - v0.2.17: disable WebView2 renderer code integrity (busy-cursor fix)
  - Cause traced after CPU/RAM idle showed 0% but cursor still spun, and
    user confirmed it persists with zero channels. The Wails docs flag a
    known WebView2 behavior: endpoint-security software (AV, Sophos, Trend
    Micro, etc.) injects DLLs into WebView2's renderer process. The
    renderer's code-integrity check trips on those unsigned/wrong-signed
    DLLs, the Event Viewer fills with "Code Integrity" errors, and the
    busy cursor pulses over the app window even when nothing is running.
  - Both apps now opt into WebviewDisableRendererCodeIntegrity in the
    Wails Windows options. Tradeoff is documented inline: malicious DLLs
    could also inject — acceptable for a chat client.

2026-05-26 15:25 - v0.2.16: reduce constant background activity (cursor-busy follow-up)
  - User reports cursor still spins constantly even with no chat — must
    be a background loop. Two suspects: live-status poll and emote-lookup
    storm.
  - ChatHub liveStatusLoop: was sequential N Helix calls every 60s, so a
    user with 12 channels had a 3-5s burst every minute. Fanned out via
    sync.WaitGroup so the burst is one HTTP RTT instead of N, and lowered
    cadence from 60s to 120s. Channel adds still trigger an immediate
    one-off live check so newly-added channels don't have to wait.
  - ChatHub renderText: emote lookups within a message used to be a
    sequential `await` per word (one Wails RPC round-trip each). Now
    collected and resolved via Promise.all, then the DOM is built. Many
    fewer IPC round-trips, less event-loop pressure for high-traffic
    messages.

2026-05-26 15:10 - v0.2.15: lazy-load badge + emote images
  - Reported: the mouse cursor goes into loading-spin mode constantly when
    chat is active. Cause: every chat message appends N badge + emote
    <img> elements that all start fetching immediately, so WebView2 is in
    near-constant network-activity state.
  - Added loading="lazy" + decoding="async" to all badge and emote
    images in both apps. The browser only fetches them as they enter the
    viewport, and decoding is off the main thread. Should kill the
    persistent busy-cursor effect during high-traffic channels.

2026-05-26 15:00 - v0.2.14: ChatHub auth pill traffic-light colors
  - Auth pill in the titlebar now color-codes the three real states:
    green @username when logged in, red "Not logged in" when never
    authenticated / explicitly logged out, amber "Session expired" when
    the saved refresh token was revoked by Twitch.
  - setLoggedInUI clears both transient classes up front so a successful
    re-login doesn't leave the amber "expired" class hanging.
  - Localized "Session expired" in DE/EN.

2026-05-26 14:55 - v0.2.13: ChatHub UI cleanup — drop bottom bar, move auth + gear up
  - Bottom status bar gone. Auth pill (@username) and the settings gear
    moved into a new dedicated titlebar above the tab bar. Window controls
    moved there too. The tab bar now only contains tabs + the add button.
  - This means tabs (with flex-wrap) can never grow into and overlap the
    right-side controls — they always sit on a separate row above.
  - Drag region moved from the tab-bar to the new titlebar so clicking
    near a tab doesn't accidentally drag the window.
  - Transient status messages ("watching #x", auth-expired, send errors)
    now appear as a small auto-hiding toast at the bottom of the chat
    area instead of a permanent bar.

2026-05-26 14:45 - v0.2.12: re-login works without an app restart in both apps
  - Bug: after the refresh token died and the user re-logged in via the
    Login button, several services were never (re)started because
    bootSequence had bailed out at boot — IRC supervisor, token refresh
    loop, helix client, YouTube auto-detect were all stuck idle.
  - cmd/wails/app.go StartLogin success now: starts runIRCSupervisor +
    tokenRefreshLoop if they were never running, kicks loadChattersAndRoles,
    spins up YouTube if configured, and re-emits the "ready" event so the
    frontend picks up the new username/channel and refreshes UI gates.
  - cmd/chathub/app.go StartLogin success: dropped the duplicate
    liveStatusLoop spawn (it was started at boot already, was leaking a
    second goroutine). Added an immediate per-channel checkAndEmitLive so
    the green live dot appears without waiting up to 60s for the next
    tick. Re-emits "ready" too.

2026-05-26 14:35 - v0.2.11: optional timestamps toggle in both apps
  - Both apps: Settings now has a "Show timestamps" checkbox. CSS-only
    hide via body.no-ts (no re-rendering needed).
  - cmd/chathub/app.go: HubConfig.HideTimestamps (stored inverted so the
    zero value keeps the historic visible default for upgrading users)
    + SetShowTimestamps bound method. GetInitialState + ready expose
    showTimestamps.
  - cmd/wails/app.go: UIConfig.ShowTimestamps already existed; added the
    bound SetShowTimestamps method and propagated showTimestamps in the
    ready event.
  - i18n entries for DE/EN in chathub.

2026-05-26 14:25 - v0.2.10: bake in the Twitch Client ID
  - User report from a fresh installation: clicking Login showed
    "Login error: no client_id configured". Cause: defaultClientID was a
    blank var meant to be set via -ldflags at build time, but the release
    build pipeline doesn't pass it, so fresh installs without a hand-edited
    config.json couldn't log in.
  - Client IDs of public OAuth apps are not secrets (only client_secret is,
    and we never use one — device-code flow doesn't need it). Embedded the
    actual ID as the default value so the very first launch can log in.
    Build-flag override still works.

2026-05-26 14:15 - v0.2.9: StreamerChat YouTube config in settings UI
  - GetYouTubeConfig + SetYouTubeConfig bound methods.
  - SetYouTubeConfig normalizes handle (prepends "@" if missing), wipes any
    stale channel_id so the next auto-detect re-resolves, and restarts the
    YT goroutine (cancelling the previous one if running).
  - Settings modal: new YouTube section with enable checkbox + handle text
    input + Save button + status line. No login needed (read-only innertube
    scrape); the explanatory text in the status line says so.

2026-05-26 14:05 - v0.2.8: StreamerChat device-code login in settings
  - Real bug exposed by the holiday-scenario chain: both apps' refresh
    tokens got revoked by Twitch (status 400 = invalid_grant on refresh).
    ChatHub already had a re-login button. StreamerChat had no login UI
    at all — the only documented path was "use the CLI version first",
    and the TUI was removed in v0.1.
  - cmd/wails/app.go: new StartLogin / OpenURL / Logout bound methods,
    mirroring chathub's device-code flow. StartLogin returns the
    verification URL + user code; PollForToken runs in a goroutine and
    fires a "loginResult" event when done.
  - On successful login, helixClient is rebuilt, badges are reloaded,
    and the IRC client is dropped so the supervisor reconnects with the
    fresh credentials.
  - When the bootSequence validate AND refresh both fail, we now emit
    "authExpired" and bail out of IRC/Helix init — previously the IRC
    supervisor would loop forever hammering Twitch with 401s.
  - Frontend: Twitch Login section in the settings modal (login button
    shows the verification URL + code; logout clears state; auth help
    text guides the user). On the authExpired event the settings modal
    auto-opens.

2026-05-26 13:50 - v0.2.7: proactive token refresh on startup, faster refresh loop
  - Scenario the user surfaced: away on holiday for 2 weeks, open the app,
    Twitch access token has long expired (and the saved one may even be
    revoked). Old behavior: validate fails -> refresh tried once -> if
    refresh also fails the user gets a silent logout. Sometimes validate
    succeeds but the token has a tiny TTL left and dies mid-session.
  - Both apps now do a proactive refresh at boot even when validate
    succeeds, so every session starts with a fresh access token.
  - tokenRefreshLoop interval reduced 60min -> 30min (Twitch access tokens
    can be valid for <1h; a 60min cadence allowed the token to die between
    ticks).
  - cmd/wails/app.go: tokenRefreshLoop now logs refresh failures instead
    of swallowing them silently.

2026-05-26 13:35 - v0.2.6: StreamerChat settings modal (gear icon in titlebar)
  - cmd/wails/autostart_windows.go + autostart_other.go: copied from chathub
    (registry key name "StreamerChat" instead of "ChatHub").
  - cmd/wails/app.go: new AutostartStatus / SetAutostart bound methods
    matching chathub's signatures.
  - index.html: replaced the static version label with a gear button in the
    titlebar; new Settings modal showing the version, a "Check for updates"
    button + Install row, and a Windows autostart checkbox (auto-hidden on
    other platforms).
  - app.js: settings open/close, manual update-check that reuses the same
    download URL state as the boot banner, autostart toggle persists
    immediately via SetAutostart.
  - style.css: settings-section / settings-row / checkbox-row styling +
    btn-primary variant.

2026-05-26 13:25 - v0.2.5: StreamerChat frameless + auto-update banner; ChatHub debug line removed
  - cmd/wails/main.go: Frameless: true.
  - cmd/wails/frontend/dist/index.html: new .titlebar element with the
    "StreamerChat" label, status pills (TW/YT), a version label, and three
    Win11-style min/max/close buttons. Drag region across the bar.
    Update banner sits below the titlebar; hidden by default, slides in
    only when CheckUpdate reports a newer release.
  - cmd/wails/frontend/dist/app.js: on boot calls GetVersion + CheckUpdate
    in the background; shows the banner with one-click Install. Install
    swaps the exe and relaunches via selfupdate.Apply + Restart.
  - cmd/wails/frontend/dist/style.css: titlebar layout, window controls
    (hover-red close), update-banner styling.
  - cmd/chathub/frontend/dist/app.js: removed the "Loaded N channels
    from <config path>" debug message from the status bar — was leaking
    the user's local %APPDATA% path into the always-visible UI. Status
    bar still surfaces transient errors and auth state.

2026-05-26 13:15 - ChatHub v0.2.4: frameless window with custom titlebar
  - cmd/chathub/main.go: Frameless: true in wails options.
  - index.html: window controls (min/max/close) integrated into the right
    side of the tab bar, plus a flex spacer that doubles as a drag region.
  - style.css: .tab-bar is the OS drag handle via `-webkit-app-region: drag`
    + `--wails-draggable: drag`. Tabs / buttons / window controls explicitly
    opt out with `no-drag` so clicks still work.
  - app.js: bindings to window.runtime.WindowMinimise / WindowToggleMaximise
    / Quit for the three buttons.
  - Result on Windows: native title-bar is gone, ~32px reclaimed at the top.
    The hover-red close button + standard min/max icons match Win11 style.

2026-05-26 13:08 - ChatHub v0.2.3: persist notification sound selection
  - The mention/live-go notification tone was a pure JS-side variable
    (`soundOnMention`). It was never sent to the backend so it reset to
    "none" on every restart. Force-Save only writes what the backend knows.
  - HubConfig: new `notif_sound` field.
  - SetNotifSound(string) bound method; "none" / "bell" / "ping".
  - GetInitialState + ready event include the persisted value.
  - Frontend: on settings close, calls SetNotifSound with the selected
    value; applyState() picks it up at boot so the selection survives
    restarts.

2026-05-26 13:05 - ChatHub v0.2.2: live beep also fires for already-live channels on boot
  - setLiveStatus only beeped on offline -> live transitions (prev === false).
    When the app started with channels already live, prev was undefined and
    the beep was suppressed.
  - Condition relaxed to `prev !== true`, so first-seen-live (initial state
    snapshot) also counts. Throttled to one beep per 800ms so opening with
    N already-live channels does not stack overlapping tones.
  - Browser audio policy caveat: AudioContext requires a user gesture, so
    the very first boot beep can be silenced if the user hasn't clicked
    anything yet. After the first click/keypress all subsequent live
    transitions are audible.

2026-05-26 13:00 - ChatHub: scroll-up now works through history
  - .chat-view had `justify-content: flex-end` + `overflow-y: auto` which
    is a known CSS pattern bug: the top of overflowing content gets clipped
    and scrolling up doesn't reach it. Replaced with normal block scroll;
    JS already pins to bottom on new messages only if the user is near the
    bottom, so the bottom-anchor UX is preserved while history is reachable.
  - Also brought back a thin visible scrollbar — fully hiding it made it
    look like the area wasn't scrollable.

2026-05-26 12:57 - v0.2.0 + in-app self-updater (both apps, Windows)
  - internal/version: shared Version constant + repo coordinates so a single
    bump propagates to both binaries.
  - internal/selfupdate: GitHub-releases-based updater. Latest() hits
    /releases/latest, Apply() downloads to <exe>.new, renames the running
    <exe> to <exe>.old (Windows allows that), atomically swaps in the new
    one, and Restart() relaunches + os.Exit's the current process.
    CleanupPrevious() removes the .old on the next launch.
  - cmd/chathub & cmd/wails: new bound methods CheckUpdate / ApplyUpdate /
    GetVersion. startup() calls selfupdate.CleanupPrevious() right after
    log init.
  - frontend/dist (chathub): "Updates" section in settings shows current
    version, a Check button that hits CheckUpdate, and an Install button
    that appears only when a newer release is published. Localized in
    DE/EN. Release-notes link opens in the user's browser.

2026-05-26 12:44 - ChatHub: beep when a channel goes live
  - setLiveStatus now plays the configured mention sound on offline->live
    transitions. Uses the existing 'soundOnMention' setting (none/bell/ping)
    so the user controls it from one place.
  - Suppressed on initial snapshot (prev === undefined) so channels that
    are already live at boot don't trigger a sound storm.

2026-05-26 12:42 - ChatHub: Chatterino-style user card on username click
  - cmd/chathub/app.go: new GetUserCard(channel, userIDOrLogin) bound method
    that aggregates Helix data: /helix/users (avatar, login, display name,
    created_at, description), /channels/followers (follower count of the
    target user, best-effort), /channels/followed (following-since for the
    target -> channel relationship), /subscriptions/user (sub tier when our
    token has permission). Returns a single map for the frontend.
  - frontend/dist/app.js: usernames in chat are now clickable; click opens
    a floating user-card modal. Per-channel ring buffer of the last 50
    messages per user is kept locally and rendered as "Recent messages"
    at the bottom of the card. Escape or backdrop click closes.
  - frontend/dist/style.css: .user-card layout (avatar, name, ID, facts,
    recent messages). Username cursor:pointer + hover underline.

2026-05-26 12:30 - ChatHub: fix race between badge lookup and channel resolution
  - After the per-channel BadgeRegistry refactor, the first render of any
    channel's history happened before resolveChannelID had populated
    channelIDCache, so LookupBadge couldn't find a userID and fell through
    to global. The frontend then cached those (wrong) global URLs per
    channel/set/version key and never refreshed.
  - app.go LookupBadge now resolves channel->userID synchronously (no-op if
    already cached) so the very first call gets the correct context.
  - loadChannelEmotes emits a new 'badgesReady' Wails event on success.
  - frontend/dist/app.js listens for badgesReady and evicts that channel's
    entries from the badge cache so subsequent lookups fetch fresh URLs.

2026-05-26 12:23 - BadgeRegistry: per-channel scope, no more cross-channel overwrite
  - internal/twitch/badges.go: the registry stored a single setID->version
    map shared across all channels. Loading thelimitger's sub badges would
    overwrite schlaubiyo's (and vice versa), so the displayed sub-tier
    artwork was whoever's LoadChannel ran last — wrong art for everyone
    else.
  - Refactored into separate `global` and `byChannel[userID]` maps.
  - Lookup now takes (channelUserID, setID, version): channel scope wins,
    global is the fallback.
  - cmd/chathub/app.go: LookupBadge(channel, setID, version) resolves the
    channel name to the cached broadcaster userID and delegates.
  - cmd/wails/app.go: same signature (always uses primary BotUserID since
    streamerchat watches one channel at a time).
  - cmd/chathub/frontend/dist/app.js: badge cache key is now
    channel/set/version so cross-channel cache hits can't pollute.
  - renderBadges takes msg.channel through to lookupBadge.

2026-05-26 12:20 - ChatHub: fix missing channel-specific badges (sub tiers, bits, founder)
  - loadChannelEmotes() marked the channel as "loaded" BEFORE the load
    actually happened. If auth wasn't ready yet (race at boot), the load
    silently returned and the channel was permanently locked out — sub
    badges and other channel-specific icons never appeared for channels
    added before the auth flow finished.
  - Fixed: only set channelEmotesLoaded[channel]=true after a successful
    LoadChannel call. Wait up to 30s for auth + resolved userID before
    giving up.
  - After successful StartLogin, re-trigger loadChannelEmotes for every
    configured channel so channels added pre-login also get their badges.

2026-05-26 12:17 - ChatHub: immediate live-status check on AddChannel
  - Previously added channels would sit without a live dot for up to 60s
    waiting on the next liveStatusLoop tick. AddChannel now fires a
    one-shot checkAndEmitLive goroutine right after the IRC join, so the
    indicator appears within ~500ms of the Helix call returning.
  - Extracted shared helper checkAndEmitLive() used by both AddChannel and
    the polling loop's per-tick check (DRY).

2026-05-26 12:13 - ChatHub: @username autocomplete
  - Per-channel chatter tracking: every incoming chat message adds the
    sender to the active channel's chatter set; existing names are bumped
    to the end of an insertion-ordered list so recent speakers rank first.
    Set is capped at 500 names per channel.
  - New floating .mention-popup anchored above the input. Triggered when
    the word at the caret begins with '@'. Tab or Enter accepts, Up/Down
    navigate, Escape closes, click also selects. Plain Enter still sends
    when the popup is closed.
  - Suggestions are recency-ranked, prefix-matched, top 8.

2026-05-26 04:32 - StreamerChat: real badge icons in the user list sidebar
  - The user list was showing colored letter prefixes (B/M/V/S) for roles
    because the chat-side LookupBadge wasn't being used for sidebar entries.
  - cmd/wails/frontend/dist/app.js: preloadRoleBadges() resolves the four
    well-known global badge URLs (broadcaster/1, moderator/1, vip/1,
    subscriber/0) from the backend BadgeRegistry once at boot, then
    re-refreshes the user list. Retries at +1.5s and +4s because backend
    badge load is async and may finish after 'ready' fires.
  - renderRoleBadge now returns an <img> when a URL is cached, and falls
    back to the colored letter while the cache is cold so the list never
    flashes empty.
  - cmd/wails/frontend/dist/style.css: new .role-badge-img sizing
    (14x14, aligned with text baseline).

2026-05-26 04:19 - ChatHub: visible join/part events, tighter tabs
  - frontend/dist/app.js: subscribe to the 'joinPart' Wails event (was being
    emitted by the backend but ignored by the frontend — the type==='join'
    handler in renderMessage was dead code). New renderJoinPart() batches
    consecutive joins/parts per channel over a 1.5s window and renders them
    as a single "X, Y, Z joined" line (Chatterino-style), capped at 8 names
    with a "+N" overflow suffix.
  - frontend/dist/style.css: tab padding 8px/14px -> 4px/10px, font-size
    14px (inherited) -> 12px; live dot 8px -> 6px, close-x and unread badge
    also shrunk so the tabs match Chatterino's compact density. New
    .msg.join-part style for the batched lines.
  - frontend/dist/i18n.js: userJoined / userParted strings in both locales.

2026-05-26 03:50 - ChatHub: DE/EN language support
  - frontend/dist/i18n.js: new tiny i18n module with two locales (en, de),
    string interpolation via {name} placeholders, dataset-driven DOM walk
    (data-i18n, data-i18n-html, data-i18n-placeholder, data-i18n-title)
  - frontend/dist/index.html: every user-facing string tagged with data-i18n
    plus a Language <select> in the settings modal
  - frontend/dist/app.js: dynamic strings (status bar, placeholders, login
    UI, history separators, deleted/cleared messages, etc.) now use i18n.t()
  - frontend/dist/app.js: language switch is immediate — on change the
    locale is persisted to hubconfig.json AND all visible dynamic text is
    refreshed without restarting the app
  - frontend/dist/app.js: i18n.localizeSystemMessage() pattern-translates
    the small set of backend system messages ("Joined #x", "Connected to
    #x") emitted by the shared internal/twitch package, so we don't have
    to touch shared code
  - cmd/chathub/app.go: HubConfig.Locale field + SetLocale bound method;
    GetInitialState + ready event include the persisted locale so the UI
    shows the right language from the very first paint

2026-05-26 03:44 - ChatHub: Windows autostart toggle in settings
  - cmd/chathub/autostart_windows.go: HKCU\Software\Microsoft\Windows\
    CurrentVersion\Run integration via golang.org/x/sys/windows/registry.
    Refuses to register %TEMP% paths (wails dev builds) so the registry
    never holds a stale path. Surfaced in Windows Settings > Apps > Startup.
  - cmd/chathub/autostart_other.go: no-op stub for non-Windows builds so
    the same App methods compile cross-platform.
  - cmd/chathub/app.go: new bound methods AutostartStatus() + SetAutostart(bool)
  - frontend/dist/index.html: new "Start ChatHub automatically with Windows"
    checkbox in the settings modal (hidden on non-Windows via JS)
  - frontend/dist/app.js: one-click toggle persists immediately on change
    (no need to close the modal first); reverts UI if the backend errors

2026-05-26 03:28 - ChatHub: wrap tabs, hide scrollbars, drag-to-reorder
  - frontend/dist/style.css: tab-bar uses flex-wrap so tabs flow onto
    multiple rows instead of horizontal scrolling
  - frontend/dist/style.css: chat-view scrollbar fully hidden (still
    scrollable, just invisible) via webkit width:0 + Firefox scrollbar-width:none
  - frontend/dist/app.js: tabs are now draggable; drop on another tab
    inserts before/after based on cursor position; visual hint via
    .dragging + .drag-over classes
  - cmd/chathub/app.go: new SetChannelOrder([]string) bound method that
    accepts the user's new order and persists it to hubconfig.json;
    AddChannel no longer alphabetically sorts (would fight the user's order)

2026-05-26 03:21 - Fix badge icons + self-echo in StreamerChat Wails app, drop TUI
  - cmd/wails/app.go: badges now load inside bootSequence AFTER token
    validate/refresh (was racing the auth flow with a stale token → 401)
  - cmd/wails/app.go: SendMessage now locally echoes the sent message via
    emitChat, because Twitch IRC does not echo PRIVMSGs back to the sender
    on the same connection (chathub gets it for free via its anon read +
    auth send dual-connection setup; streamerchat uses a single connection)
  - cmd/wails/app.go: token refresh loop reloads badges with the fresh token
  - cmd/wails/app.go: added [AUTH] logging on validate/refresh paths
    (previously silent failures)
  - Synced ~/.config/streamerchat/config.json token to a valid one (the
    stored access_token was expired, which masked the real bugs)
  - Removed TUI: deleted cmd/streamerchat/, internal/tui/, cmd/launcher/,
    StreamerChat.bat, and the macOS/Windows TUI binaries — Wails GUI is
    the only client going forward


  - Click on any username in chat to open mod action bar
  - Mod actions: Ban, Timeout 10m/1h/24h, Delete message, Unban
  - Actions wired to Twitch Helix API (executes in background)
  - Visual feedback: system message confirms action
  - ESC or click elsewhere to close mod bar
  - Unified single chat feed (removed split-view, all messages in one panel)
  - ChatView now tracks click regions for username hit-testing

2026-04-09 15:15 - Add YouTube Auto-Detect live streams
  - Added internal/youtube/detect.go
    - LiveDetector polls channel's /live page every 30s
    - Detects when channel goes live, extracts video ID automatically
    - Detects when stream ends, disconnects YouTube chat
    - No API key needed (scrapes channel page like a browser)
  - Updated config: added youtube.channel_handle field (@username)
  - Updated main.go:
    - YouTube auto-connects when live stream detected
    - Auto-disconnects when stream ends
    - First-time setup now asks for YouTube channel handle
    - Supports both auto-detect and manual video_id override
  - Fixed Twitch auth: authorization_pending message parsing (underscore vs space)

2026-04-09 14:30 - Add Twitch Device Code OAuth flow
  - Added internal/twitch/auth.go with full Device Code Grant flow
    - RequestDeviceCode: starts flow, returns user code + verification URL
    - PollForToken: polls until user authorizes on Twitch
    - ValidateToken: checks if token is still valid, returns username/user ID
    - RefreshAccessToken: refreshes expired tokens automatically
  - Updated main.go with interactive first-time setup:
    - Prompts for Client ID and channel name
    - Runs Device Code flow: "Go to URL, enter code"
    - Saves credentials to config automatically
    - On subsequent runs: validates token, auto-refreshes if expired
  - No more manual token copy-paste needed

2026-04-09 14:25 - Initial project setup
  - Initialized Go module (github.com/miwi/streamerchat)
  - Created project structure: cmd/streamerchat, internal/{twitch,youtube,chat,tui,config}
  - Added unified chat message types (internal/chat/message.go)
    - Platform enum (Twitch/YouTube), MessageType enum, Badge struct, Message struct
    - UserJoinPart and ChatSettings types
  - Added Twitch IRC client (internal/twitch/irc.go)
    - IRC connection via gempir/go-twitch-irc v4
    - JOIN/PART event forwarding
    - PRIVMSG parsing with badges, emotes, bits, colors
    - CLEARCHAT/CLEARMSG handling (bans, timeouts, message deletes)
    - USERNOTICE handling (subs, giftsubs, raids, announcements)
    - ROOMSTATE handling (slow mode, sub-only, etc.)
    - Auto-reconnect support
  - Added Twitch Helix API client (internal/twitch/helix.go)
    - Ban/Unban/Timeout users
    - Delete messages, Clear chat
    - Set slow mode, sub-only, emote-only, follower-only
    - Get chat settings
  - Added YouTube InnerTube chat client (internal/youtube/innertube.go)
    - Unofficial InnerTube API (no quota limits)
    - Fetches ytInitialData and continuation tokens from live chat page
    - Polling loop with server-dictated intervals
    - Parses text messages, Super Chats, memberships
    - Badge parsing (Moderator, Owner, Member)
  - Added TUI with Bubble Tea v2 (internal/tui/)
    - styles.go: Color palette, platform badges, role badges, layout styles
    - chatview.go: Scrollable chat view with message rendering
    - app.go: Main Bubble Tea model with:
      - Split view support (side-by-side or stacked)
      - Mouse support (click panel focus, scroll wheel)
      - Keyboard controls (Tab: target, PgUp/PgDn: scroll, F1/F2: panel)
      - Send target cycling (Twitch / YouTube / Both)
      - Status bar with chat mode indicators
      - Visual distinction: purple TW badge vs red YT badge
  - Added config management (internal/config/config.go)
    - JSON config at ~/.config/streamerchat/config.json
    - Twitch, YouTube, and UI settings
    - Setup guide printed on first run
  - Added main entry point (cmd/streamerchat/main.go)
    - Connects Twitch IRC and YouTube InnerTube clients
    - Forwards messages to TUI via Bubble Tea channels
  - Dependencies: charm.land/bubbletea v2.0.2, charm.land/lipgloss v2.0.2,
    gempir/go-twitch-irc v4.4.1, nicklaw5/helix v2.32.0
